import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Trial Balance Calculator"
};

export default function TrialBalanceCalculatorPage() {
  return <div>Trial Balance Calculator page placeholder.</div>;
}
